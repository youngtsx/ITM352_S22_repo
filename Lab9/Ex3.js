/*var attributes = "Tiffany;20;MIS";
name = "Tiffany";
age = 20;

var attributes = name + ";" + age + ";" + (age + 0.5) + (0.5-age);

var parts = attributes.split(";");

for(part of parts) {
    console.log(parts)
}*/